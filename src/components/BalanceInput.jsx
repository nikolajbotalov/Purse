import React from 'react';

const BalanceInput = ({ placeholder }) => {
  return (
    <>
      <input placeholder={placeholder} className="balance-input" />
    </>
  );
};

export default BalanceInput;
